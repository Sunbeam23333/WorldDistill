{
    "distill_method": "consistency_distill",
    "description": "Trajectory Consistency Distillation (TCD). Semi-linear consistency function mapping any point on the ODE trajectory to the origin.",
    "num_inference_steps": 2,
    "ema_decay": 0.9999,
    "consistency_loss_type": "huber",
    "huber_c": 0.001,
    "optimizer": "adamw",
    "learning_rate": 1e-5,
    "weight_decay": 0.0,
    "max_grad_norm": 1.0,
    "warmup_steps": 500,
    "mixed_precision": "bf16",
    "gradient_checkpointing": true,
    "notes": "Reference: Trajectory Consistency Distillation (TCD), Latent Consistency Distillation (LCD)"
}
